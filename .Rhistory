?with.mids
?mira
?as.mira
install.packages("roxygen2")
install.packages("V:/UserData/600003/r-package/ordinalimputation_0.1.0.tar.gz", repos = NULL, type = "source")
libary(ordinalimputation)
library(ordinalimputation)
detach("package:ordinalimputation", unload=TRUE)
library(ordinalimputation)
# To install the package: devtools::install_github("DaanNieboer/ordinalimputation")
library(ordinalimputation)
dta <- wine
# Generate some missing values:
dta$temp[sample(1:nrow(dta), size = 10)] <- NA
imp <- mice(dta)
res <- with(imp, clmm(rating ~ temp + contact + (1|judge), data = wine))
est <- pool.clmm(res)
est
plot(est)
plot(est, main = "Test")
library(ordinalimputation)
# To install the package: devtools::install_github("DaanNieboer/ordinalimputation")
library(ordinalimputation)
dta <- wine
# Generate some missing values:
dta$temp[sample(1:nrow(dta), size = 10)] <- NA
imp <- mice(dta)
res <- with(imp, clmm(rating ~ temp + contact + (1|judge), data = wine))
est <- pool.clmm(res)
est
est
names(est)
est$fixed_effects
print(est)
est$random_dist
print(est)
library(ordinalimputation)
dta <- wine
# Generate some missing values:
dta$temp[sample(1:nrow(dta), size = 10)] <- NA
imp <- mice(dta)
res <- with(imp, clmm(rating ~ temp + contact + (1|judge), data = wine))
est <- pool.clmm(res)
est
?pool.clmm
library(ordinal)
?clmm
